-----BEGIN\s(RSA |OPENSSH |EC |DSA |PGP )?PRIVATE KEY( BLOCK)?-----
AGE-SECRET-KEY-1[A-Z0-9]+

gh[pousr]_[A-Za-z0-9]{36,}
glpat-[A-Za-z0-9\-_]{20,}

(api|secret|token|password|passwd|pwd)[_-]?(key)?\s*[:=]\s*['\"]?[A-Za-z0-9+/=_\-]{16,}['\"]?

tskey-[a-zA-Z0-9-]+

CACHIX_AUTH_TOKEN
cachix\.org.*authToken

secrets\.yaml$
\.age$

GNUPGHOME

(password|secret|apiKey|token)\s*=\s*"[^"$]{8,}"
